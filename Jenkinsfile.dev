pipeline {
    agent any

    stages {
        stage('Descarga de Fuentes') {
            steps {
                script {
                    deleteDir()
                }
            }
        }

        stage('Configurar gcloud') {
            steps {
                script {
                sh 'gcloud init'
                }
            }
        }

    }
}
